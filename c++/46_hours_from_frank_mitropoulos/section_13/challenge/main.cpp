#include "movie.cpp"
#include "movies.cpp"
#include <vector>

int main()
{
  // Movie movie1{"A", "PG-13", 0};
  // Movie movie2{"C", "PG", 0};
  Movies movies;
  //
  // movies.display_movies();
  // movies.add_movie(movie1);
  // movies.add_movie(movie1);
  // movies.display_movies();
  movies.add_movie(Movie{"B", "PG", 0});
  // movies.display_movies();
  // movies.increase_watched(movie1);
  // movies.display_movies();
  // movies.increase_watched(movie2);

  // std::vector<Movie> vec;
  // vec.push_back(Movie{"B", "PG", 0});
}
